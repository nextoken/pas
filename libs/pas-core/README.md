# pas-core

Core logic for the PAS toolkit, shared between the CLI and the Console.

This package lives under the **PAS** repository at `libs/pas-core/`. The historical standalone `pas-core` repository may be archived or read-only; develop and release `pas-core` together with PAS.
